import React from 'react'
import ReactDOM from 'react-dom/client'
import ContextualApp from '../contextual-prm-app.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ContextualApp />
  </React.StrictMode>,
)